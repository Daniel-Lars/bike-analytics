dbt_project:
  outputs:
    dev:
      dbname: "{{ env_var('DEV_POSTGRES_DB') }}"
      host: localhost
      pass: "{{ env_var('DEV_POSTGRES_PASSWORD') }}"
      port: 5432
      schema: my_schema
      threads: 1
      type: postgres
      user: "{{ env_var('DEV_POSTGRES_USER') }}"
  target: dev
